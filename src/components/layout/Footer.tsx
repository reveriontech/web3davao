export function Footer() {
  return (
    <footer
      style={{
        padding: '2rem var(--container-padding)',
        borderTop: '1px solid var(--color-border)',
        marginTop: 'auto',
      }}
    >
      <div className="container text-center">
        <small style={{ color: 'var(--color-text-muted)' }}>Footer</small>
      </div>
    </footer>
  )
}
